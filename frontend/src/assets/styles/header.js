const HeaderStyle = theme => {
  return {
    headerContainer: {
      width: 600,
      alignItems: "center",
      justifyContent: "center",
      margin: theme.spacing(2, 0)
    },
    toggleContainer: {
      margin: theme.spacing(2, 0)
    }
  };
};

export default HeaderStyle;
