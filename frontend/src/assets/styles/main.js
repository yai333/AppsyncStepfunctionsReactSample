const MainStyle = theme => {
  return {
    root: {
      display: "flex",
      alignItems: "center",
      flexDirection: "column",
      justifyContent: "space-around",
      overflow: "hidden",
      backgroundColor: "#444"
    }
  };
};

export default MainStyle;
